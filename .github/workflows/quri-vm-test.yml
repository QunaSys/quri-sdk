name: QURI VM Test

on:
  pull_request:
    paths:
      - 'quri-vm/**.py'
      - poetry.lock
      - .github/workflows/quri-vm-test.yml
  push:
    branches: [main]

defaults:
  run:
    working-directory: quri-vm

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - uses: ./.github/actions/quri-vm/setup
      with:
        poetry-version: '2.2.1'
        python-version: '3.10'

    - run: |
        poetry env use 3.10
        poetry install -vvv --only dev --extras vm

    - run: poetry run pytest quri-vm
