[build-system]
requires = ["hatchling>=1.27", "uv-dynamic-versioning"]
build-backend = "hatchling.build"

[project]
name = "quri-parts-pyscf"
dynamic = ["version"]
description = "A plugin to use PySCF with QURI Parts"
license = "Apache-2.0"
authors = [{ name = "QURI Parts Authors", email = "opensource@qunasys.com" }]
readme = "README.md"
keywords = ["quantum", "quantum computing"]
classifiers = [
    "Topic :: Scientific/Engineering :: Physics",
    "Typing :: Typed"
]
requires-python = ">=3.10,<4.0"
dependencies = [
    "typing-extensions>=4.1.1,<5.0.0",
    "numpy>=1.22.0",
    # pyscf 2.4.0+ ships a py3-none wheel (ctypes-loaded C libs) that works
    # across supported Python versions; 2.3.x had no 3.14-compatible wheel.
    "pyscf>=2.4.0,<3.0.0",
    "quri-parts-core",
    "quri-parts-chem",
]

[project.urls]
Repository = "https://github.com/QunaSys/quri-sdk"
Documentation = "https://quri-parts.qunasys.com"

[tool.hatch.version]
source = "uv-dynamic-versioning"

[tool.uv-dynamic-versioning]
style = "pep440"
# Release CI bakes the resolved version in here before uv build so that
# sdists carry the correct version when built without git metadata.
fallback-version = "0.0.0"

[tool.hatch.build.targets.wheel]
packages = ["quri_parts"]

[tool.hatch.build.targets.sdist]
only-include = ["quri_parts"]

[tool.uv.sources]
quri-parts-core = { path = "../core", editable = true }
quri-parts-chem = { path = "../chem", editable = true }
