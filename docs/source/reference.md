# API Reference

::::{only} has_api_ref
The QURI Parts API reference is a separate build served under
<a href="/api/index.html"><code>/api/</code></a>.

On the deployed site you are redirected there automatically (see the
`/reference` rule in `netlify.toml`), so this page is not shown.
::::

::::{only} not has_api_ref
The QURI Parts API reference is built and served only in the deployed site.
To preview it locally, run `make api`, then rebuild.
::::
